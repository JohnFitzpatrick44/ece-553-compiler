/* var 123
	/* var 123 */
	var 123	
	is still a comment
*/

function readint(any: any) : int =
  let var i := 0
      /* random comment */
      function isdigit(s : string) : int = ord(buffer)>=ord("0") & ord(buffer)<=ord("9")
      /* random comment */
      function skipto() =
      	/* random comment */
        while buffer=" " | buffer="\n"
          do buffer := getchar()
   in skipto();
      /* random comment */
      any.any := isdigit(buffer);
      while isdigit(buffer)
        do (i := i*10+ord(buffer)-ord("0"); buffer := getchar());
      i
      /* random comment */
  end

/* var 123 /* var 123 /* var 123 */ var 123 */ var 123 */
