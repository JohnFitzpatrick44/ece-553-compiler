let 
  type treeNode = {v: int, left: treeNode, right: treeNode}

  var root: treeNode := treeNode {v=1, 
                                  left=nil, 
                                  right=treeNode {v=2, left=nil, right=nil}}
  function f(i: int) = f(root.v)
in
  f(10)
end
